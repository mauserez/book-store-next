"use client";
import { Item } from "@/src/entities/catalog";
import s from "./ItemList.module.css";
/* import { useAppSelector } from "@/src/shared/redux/hooks";
import { selectItems } from "@/src/shared/redux/slices/catalogSlice"; */

export default function ItemList() {
	return 123;
	/* const itemList = Item[];

	return (
		<div className={s.itemListWrap}>
			<div className={s.itemList}>
				{itemList.map((book) => {
					return <Item key={book.id} {...book} />;
				})}
			</div>
			<button className={s.loadMore} type="button">
				load more
			</button>
		</div>
	); */
}
